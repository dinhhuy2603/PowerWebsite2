<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta content="A fully featured admin theme which can be used to build CRM, CMS, etc." name="description" />
    <meta content="Coderthemes" name="author" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <!-- favicon
        ============================================ -->
    <link rel="shortcut icon" href="~/Content/images/favicon.ico">
    <!-- Google Fonts
        ============================================ -->
    <link href="https://fonts.googleapis.com/css?family=Roboto:100,300,400,700,900" rel="stylesheet">
    <title>@ViewBag.Title - Power Monitoring</title>
    @*@Styles.Render("~/Content/css")*@
    @RenderSection("Styles", false)
    <style>
        body[data-sidebar-size=condensed] .left-side-menu #sidebar-menu > ul > li:hover > .collapse > ul {
            width: 300px;
        }
    </style>
</head>
<body class="loading">
    <!-- Begin page -->
    <div id="wrapper">
        <!-- Topbar Start -->
        <div class="navbar-custom">
            <div class="container-fluid">
                <ul class="list-unstyled topnav-menu float-right mb-0">

                    <li class="notification-list d-flex justify-content-center align-items-center">
                        <a class="nav-link nav-user mr-0 waves-effect waves-light">
                            <img src="~/Content/images/users/Admin-User-256.png" alt="user-image" class="rounded-circle">
                            <span class="pro-user-name ml-1">
                                @Session["user_name"]
                            </span>
                        </a>
                        <a href="javascript:void(0);" class="nav-link waves-effect waves-light" data-toggle="modal" data-target="#logoutModal">
                            <i class="fe-log-out"></i>
                            <span>Logout</span>
                        </a>
                        <div class="logo-box" style="width: 140px">
                            <a href="#" class="logo logo-light text-center">
                                <span class="logo-sm">
                                    <img src="~/Content/images/logo-2-rs.png" alt="user-image" width="50">
                                </span>
                                <span class="logo-lg">
                                    <img src="~/Content/images/logo-2-rs.png" alt="user-image">
                                </span>
                            </a>
                        </div>
                    </li>
                </ul>

                <!-- LOGO -->
                <div class="logo-box">
                    <a href="#" class="logo logo-light text-center">
                        <span class="logo-sm">
                            <img src="~/Content/images/logo.png" alt="user-image" width="50">
                        </span>
                        <span class="logo-lg">
                            <img src="~/Content/images/logo.png" alt="user-image">
                        </span>
                    </a>
                </div>

                <ul class="list-unstyled topnav-menu topnav-menu-left m-0">
                    <li>
                        <button class="button-menu-mobile waves-effect waves-light">
                            <i class="fe-menu"></i>
                        </button>
                    </li>

                    <li>
                        <!-- Mobile menu toggle (Horizontal Layout)-->
                        <a class="navbar-toggle nav-link" data-toggle="collapse" data-target="#topnav-menu-content">
                            <div class="lines">
                                <span></span>
                                <span></span>
                                <span></span>
                            </div>
                        </a>
                        <!-- End mobile menu toggle-->
                    </li>

                    <li class="dropdown d-none d-xl-block">
                        <a class="nav-link" href="#">
                            Dashboard
                        </a>
                    </li>
                </ul>
                <div class="clearfix"></div>
            </div>
        </div>
        <!-- end Topbar -->
        <!-- ========== Left Sidebar Start ========== -->
        <div class="left-side-menu">
            <div class="h-100" data-simplebar>
                <!-- User box -->
                <div class="user-box text-center">
                    <img src="~/Content/images/users/user-1.jpg" alt="user-img" title="Mat Helme"
                         class="rounded-circle avatar-md">
                    <div class="dropdown">
                        <a href="javascript: void(0);" class="text-dark h5 mt-2 mb-1 d-block">Geneva Kennedy</a>
                        <a href="javascript:void(0);" class="notify-item">
                            <i class="fe-log-out mr-1"></i>
                            <span>Logout</span>
                        </a>

                    </div>
                    <p class="text-muted">Admin Head</p>
                </div>

                <!--- Sidemenu -->
                <div id="sidebar-menu">

                    <ul id="side-menu">
                        <li>
                            <a href="@Url.Action("Index","Home")">
                                <i class="fas fa-bolt"></i>
                                <span>Energy Overview</span>
                            </a>
                        </li>
                        @*<li>
                            <a href="@Url.Action("Dashboard","Home")">
                                <i class="fas fa-chart-bar"></i>
                                <span> Dashboard</span>
                            </a>
                        </li>
                        <li>
                            <a href="@Url.Action("Overview","Home")">
                                <i class="nav-icon fas fa-tachometer-alt"></i>
                                <span> Overview </span>
                            </a>
                        </li>*@
                        <li>
                            <a href="#menu2" data-toggle="collapse">
                                <i class="nav-icon fas fa-chart-pie"></i>
                                <span> Chart Online</span>
                                <span class="menu-arrow"></span>
                            </a>
                            <div class="collapse" id="menu2">
                                <ul class="nav-second-level">
                                    <li>
                                        <a href="@Url.Action("ChartCb1", "Home")">Chart CB1</a>
                                    </li>
                                    <li>
                                        <a href="@Url.Action("ChartCb2", "Home")">Chart CB2</a>
                                    </li>
                                    <li>
                                        <a href="@Url.Action("ChartCb3", "Home")">Chart CB3</a>
                                    </li>
                                </ul>
                            </div>
                        </li>
                        <li>
                            <a href="@Url.Action("Chart","Report")">
                                <i class="nav-icon far fa-calendar-alt"></i>
                                <span>Chart Report</span>
                            </a>
                        </li>
                        <li>
                            <a href="@Url.Action("Index","Alarm")">
                                <i class="nav-icon fas fa-bell"></i>
                                <span>Alarm Tables</span>
                            </a>
                        </li>
                        <li>
                            <a href="@Url.Action("Index","Report")">
                                <i class="nav-icon fas fa-table"></i>
                                <span>Report Tables</span>
                            </a>
                        </li>
                        <li>
                            <a href="@Url.Action("Index","Setting")">
                                <i class="nav-icon fas fa-edit"></i>
                                <span>Setting</span>
                            </a>
                        </li>
                    </ul>

                </div>
                <!-- End Sidebar -->

                <div class="clearfix"></div>

            </div>
            <!-- Sidebar -left -->

        </div>
        <!-- Left Sidebar End -->
        <!-- ============================================================== -->
        <!-- Start Page Content here -->
        <!-- ============================================================== -->

        @RenderBody()
        <!-- ============================================================== -->
        <!-- End Page content -->
        <!-- ============================================================== -->


    </div>

    <!-- Logout Modal-->
    <div class="modal fade" id="logoutModal" tabindex="-1" role="dialog" aria-labelledby="exampleModalLabel" aria-hidden="true">
        <div class="modal-dialog" role="document">
            <div class="modal-content">
                <div class="modal-header">
                    <h5 class="modal-title" id="exampleModalLabel">Đăng xuất</h5>
                    <button class="close" type="button" data-dismiss="modal" aria-label="Close">
                        <span aria-hidden="true">×</span>
                    </button>
                </div>
                @*<div class="modal-body">Select "Logout" below if you are ready to end your current session.</div>*@
                <div class="modal-footer">
                    <button class="btn btn-secondary" type="button" data-dismiss="modal" style="font-size: 14px !important">Hủy</button>
                    @*<a class="btn btn-primary" href="@Url.Action("ChangePassword", "Account", new { id = Session["UserID"].ToString() })" style="font-size: 0.875rem !important">Đổi mật khẩu</a>*@
                    <a class="btn btn-primary" href="@Url.Action("Logout", "Account")" style="font-size: 14px !important">Đăng xuất</a>
                </div>
                @*style="font-size: 0.875rem !important"*@
            </div>
        </div>
    </div>
    <!-- END wrapper -->
    <div class="rightbar-overlay"></div>
    @*@Scripts.Render("~/bundles/js")*@
    @RenderSection("scripts", required: false)
    <script>
        addCommas = function (input) {
            // If the regex doesn't match, `replace` returns the string unmodified
            return (input.toString()).replace(
            // to the function; in this case, every argument after 'match'
            /^([-+]?)(0?)(\d+)(.?)(\d+)$/g, function (match, sign, zeros, before, decimal, after) {
                var reverseString = function (string) { return string.split('').reverse().join(''); };
                var insertCommas = function (string) {

                    // Reverse, because it's easier to do things from the left
                    var reversed = reverseString(string);

                    // Add commas every three characters
                    var reversedWithCommas = reversed.match(/.{1,3}/g).join(',');

                    // Reverse again (back to normal)
                    return reverseString(reversedWithCommas);
                };
                return sign + (decimal ? insertCommas(before) + decimal + after : insertCommas(before + after));
            }
          );
        };
    </script>
</body>
</html>
